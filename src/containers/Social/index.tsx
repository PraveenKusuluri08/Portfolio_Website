'use client';

import Sidebar from '@/components/ui/Sidebar';

import SocialLinks from './SocialLinks';

const Social = () => {
  return (
    <Sidebar>
      <SocialLinks />
    </Sidebar>
  );
};

export default Social;
